<%- include('../partials/header.ejs') %>

	<h2><%= user.username %></h2>

	<div>
		<img src="/<%= user.profilePhoto %>" height="300">
	</div>

	<div>
		<p>age: <%= user.age %></p>
	</div>
	<div>
		<p>location: <%= user.location %></p>
	</div>

	<div>
		<p>events attended:</p>
		<!-- loop here -->
	</div>

	<div>
		<p>photos uploaded:</p>
		<!-- loop here -->
	</div>
<% if (userId == user._id) {%>
		<p>(<a href="/users/<%= user._id %>/edit">EDIT</a>)</p>
	<%}%>
<%- include('../partials/footer.ejs') %>